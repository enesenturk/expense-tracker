namespace ExpenseTracker.MobileApp.Helpers
{
	public class ObservableListHelper
	{

		//public static ObservableCollection<T> Update<T>(ObservableCollection<T> records) where T : ListRecordModel
		//{
		//	ObservableCollection<T> newRecords = new ObservableCollection<T>();

		//	for (int i = 0; i < records.Count; i++)
		//	{
		//		T record = records[i];

		//		record.Index = i;
		//		record.RowColor = record.Index % 2 == 0 ? ColorConstants.MiddlePurple : ColorConstants.SoftPurple;

		//		newRecords.Add(record);
		//	}

		//	return newRecords;
		//}

	}
}
